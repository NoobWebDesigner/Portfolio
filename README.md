# Portfolio

This project originally used Pug templates. The new `astro/` directory contains an initial setup for migrating the site to [Astro](https://astro.build/).

To develop the Astro version:

```bash
cd astro
npm install
npm run dev
```

The generated site continues to use the existing `public` directory for static assets.

